// 17.7 Given any integer, print an English phrase that describes the integer (e.g., "OneThousand, Two Hundred Thirty Four").

/* TODO
#include <iostream>
using namespace std;

string printeng(int x) {
	int digit;
	int place = 0;
	string s;
	while (x != 0) {
		digit = x%10;
		switch (place) {
			case 0:
				strcat(s, arr[digit]);
		}
	}
}
*/
